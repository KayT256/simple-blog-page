// Only when scrolling that the scrollbar is displayed. Otherwise, hide it.

// window.addEventListener('load', () => {
//     let timer;
//     document.querySelectorAll('.scroll').forEach(element => {
//         element.addEventListener('scroll', () => {
//             element.classList.add('show-scroll');
//             timer = setTimeout(timer);
//             timer = setTimeout(() => {
//                 element.classList.remover('show-scroll')
//             }, 1000)
//         })
//     })
// })